# Cloudflare bindings (set via wrangler secrets or vars)
# These are automatically injected in Workers/Functions runtime

# Stripe
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_ONE_OFF=
STRIPE_PRICE_WEEKLY=
STRIPE_PRICE_MONTHLY=
STRIPE_PRICE_QUARTERLY=

# Cloudflare Email Workers (magic links)
FROM_EMAIL=noreply@opinionatedimagen.com

# Public
PUBLIC_API_BASE=/api
